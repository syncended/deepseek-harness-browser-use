# DeepSeek Harness Browser Use

Persistent Playwright browser and an interactive browser panel for DeepSeek Harness.

Status: MVP.

Server deployments require Chromium. Remote Web control follows DSH `trustedHosts`; enable `noSandbox` only inside an isolated root-run container.
